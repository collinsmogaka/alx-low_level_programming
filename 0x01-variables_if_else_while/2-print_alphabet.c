#include <stdio.h>

/**
* main - main block
*description: get a random number and print number
*Return: Always (Success)
*/

int main(void)
{
char c;
for (c = 'a'; c <= 'Z'; c++)
putchar(c);
putchar('\n');
return (0);
}
